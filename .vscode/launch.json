{
  "version": "0.2.0",
  "configurations": [{
    "name": "Run T-Code demo",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/demo-project"]
  }]
}
